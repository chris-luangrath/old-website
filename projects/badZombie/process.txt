http://stackoverflow.com/questions/17176046/pause-function-until-enter-key-is-pressed-javascript
var ifReply = false;
var wait4Enter = true;
do {
    Display Narrator
    Display Options
    keypress e or whatever
    while (e === 13) {
        Wait until enter is pressed
        Read Options
        If actual reply {
            ifReply = true
            Branch off
        }
    }
} while (ifReply === false);

